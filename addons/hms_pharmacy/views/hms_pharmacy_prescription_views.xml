<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Prescriptions tree -->
    <record id="view_hms_prescription_list" model="ir.ui.view">
        <field name="name">hms.prescription.list</field>
        <field name="model">hms.prescription</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="patient_id"/>
                <field name="doctor_id"/>
                <field name="date"/>
            </list>
        </field>
    </record>

    <!-- Prescriptions form -->
    <record id="view_hms_prescription_form" model="ir.ui.view">
        <field name="name">hms.prescription.form</field>
        <field name="model">hms.prescription</field>
        <field name="arch" type="xml">
            <form string="Prescription">
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="patient_id"/>
                        <field name="doctor_id"/>
                        <field name="date"/>
                    </group>
                    <group>
                        <field name="medicine_ids" widget="many2many_tags"/>
                    </group>
                    <notebook>
                        <page string="Notes">
                            <field name="note"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action for prescriptions -->
    <record id="action_hms_prescriptions" model="ir.actions.act_window">
        <field name="name">Prescriptions</field>
        <field name="res_model">hms.prescription</field>
        <field name="view_mode">list,form</field>
        <field name="view_id" ref="view_hms_prescription_list"/>
    </record>
</odoo>
