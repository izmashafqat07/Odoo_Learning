<odoo>
    <template id="library_request_form" name="Library Request Form">
        <t t-call="website.layout">
            <div id="wrap" class="container mt-5">
                <h1 class="mb-4">Request a Book</h1>
                <form action="/library/request/submit" method="post">
                    <div class="form-group mb-3">
                        <label>Your Name</label>
                        <input type="text" class="form-control" name="name" required="required"/>
                    </div>
                    <div class="form-group mb-3">
                        <label>Your Email</label>
                        <input type="email" class="form-control" name="email" required="required"/>
                    </div>
                    <div class="form-group mb-3">
                        <label>Book Title</label>
                        <input type="text" class="form-control" name="book_title" required="required"/>
                    </div>
                    <div class="form-group mb-3">
                        <label>Notes</label>
                        <textarea class="form-control" name="notes" rows="4"></textarea>
                    </div>
                    <button type="submit" class="btn btn-primary">Submit Request</button>
                </form>
            </div>
        </t>
    </template>

    <template id="library_request_thankyou" name="Library Request Thank You">
        <t t-call="website.layout">
            <div id="wrap" class="container mt-5">
                <h1>Thank you</h1>
                <p>Your request was submitted.</p>
            </div>
        </t>
    </template>
</odoo>
